Démo avant/après du hero (page d'accueil).

Fichiers actuellement utilisés (référencés dans src/components/home/HeroDemo.tsx) :
  demo-before.webp  →  photo de corps SANS tatouage (dos nu)
  demo-after.png    →  rendu IA correspondant (dos avec les roses)

Pour changer le visuel : remplace ces 2 fichiers (garde les mêmes noms),
ou mets à jour BEFORE_SRC / AFTER_SRC dans HeroDemo.tsx si tu changes les noms.
Format portrait ~4/5 recommandé.
