import { SignupForm } from '@/components/auth/SignupForm'

export const metadata = {
  title: 'Inscription — Memories°',
  robots: { index: false },
}

export default function InscriptionPage() {
  return <SignupForm />
}
